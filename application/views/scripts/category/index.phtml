<h1>Categories</h1>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <?php if ($this->categories): ?>
        <table class="table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th><span class="pull-right">Action</span></th>
                </tr>
            </thead>
            <tbody>
            <?php foreach ($this->categories as $category): ?>
                <tr>
                    <td><?php echo $category->name; ?></td>
                    <td>
                        <div class="btn-group pull-right" role="group">
                            <a href="<?php echo $this->url(array('controller' => 'category', 'action' => 'edit', 'id' => $category->id), null, true); ?>" class="btn btn-default btn-xs">Edit</a>
                            <a href="<?php echo $this->url(array('controller' => 'category', 'action' => 'delete', 'id' => $category->id), null, true); ?>" class="btn btn-default btn-xs">Delete</a>
                        </div>
                    </td>
                </tr>
            <?php endforeach; ?>
            </tbody>
        </table>
        <?php endif; ?>
        <a href="<?php echo $this->url(array('controller' => 'category', 'action' => 'edit'), null, true); ?>" class="btn btn-default btn-sm pull-right">Add Category</a>
    </div>
</div>
